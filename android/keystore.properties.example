# Copy to keystore.properties (gitignored) and use the passwords you chose when creating the keystore.
# See docs/FIREBASE_ANDROID.md — keystore default path: mobile/obostores-release.keystore

storeFile=../mobile/obostores-release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=obostores
keyPassword=YOUR_KEY_PASSWORD
