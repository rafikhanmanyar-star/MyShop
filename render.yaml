databases:
  - name: Myshop-db-staging
    databaseName: myshop
    user: myshop_user

services:
  - type: web
    name: MyShop-Api
    env: node
    plan: starter
    branch: main
    buildCommand: cd server && npm install --include=dev && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: CORS_ORIGIN
        value: "*"
      - key: MOBILE_APP_URL
        value: https://myshop-mobile.onrender.com
      - key: RIDER_APP_URL
        value: https://myshop-rider.onrender.com
      # Driving ETA (Directions API) + optional server geocoding; create in Render dashboard if unset here.
      - key: GOOGLE_MAPS_API_KEY
        sync: false
    disk:
      name: myshop-uploads
      mountPath: /opt/render/project/src/server/uploads
      sizeGB: 1

  - type: web
    name: MyShop-Rider
    runtime: static
    branch: main
    rootDir: rider
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      # Must match your API public URL + /api (same shape as mobile/.env.production.example).
      - key: VITE_API_URL
        value: https://myshop-api-9pd4.onrender.com/api
      # Maps JavaScript + Directions (rider order screen). Set the real key in the Render dashboard.
      - key: VITE_GOOGLE_MAPS_API_KEY
        sync: false

  - type: web
    name: MyShop-Mobile
    runtime: static
    branch: main
    rootDir: mobile
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      # Customer PWA — same API base as rider; update host if your API service name differs.
      - key: VITE_API_URL
        value: https://myshop-api-9pd4.onrender.com/api