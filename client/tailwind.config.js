/** Tailwind v4: theme tokens live in `src/index.css` via `@theme`. This file enables `darkMode: 'class'` on `<html>` and documents the setup. */
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
};
